{
  "uischema": {
    "type": "Control",
    "scope": "#/properties/apiBaseUrl",
    "label": "API Base URL",
    "options": {
      "description": "Base URL of the IE Device + /ih subpath for the Intelligence Hub, e.g. https://your-ied-hostname.com/intelligence-hub/"
    }
  },
  "dataschema": {
    "type": "object",
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "format": "uri",
        "default": "https://your-ied-hostname.com/intelligence-hub/",
        "description": "Must be a fully qualified http(s) URL",
        "minLength": 1
      }
    },
    "required": ["apiBaseUrl"]
  }
}
